{{#extend "layout"}}
    {{#content "main"}}

        <section class="services">
            <div class="container">
                <div class="services__wrapper">
                    <div class="services__body">
                        <ul class="services__list">
                            {{#each services }}
                                <li class="services__item">
                                    <h2 class="services__title">{{this.title}}</h2>
                                    <p class="services__text">{{this.text}}</p>
                                </li>
                            {{/each}}

                        </ul>
                    </div>
                </div>
            </div>
        </section>

    {{/content}}
{{/extend}}